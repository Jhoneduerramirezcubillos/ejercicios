//elaciones. Construir una interfaz IRelaciones (y posteriormente una clase que la implemente) que
incluya los siguientes métodos:
- boolean esMayor(Object b) ; // Devuelve verdadero si a es mayor que b
- esMenor(Object b) ; // Devuelve verdadero si a es menor que b boolean
- boolean esIgual(Object b) ; // Devuelve verdadero si a es igual que b *
public interface IRelaciones {
    boolean esMayor(Object b); // Devuelve verdadero si a es mayor que b
    boolean esMenor(Object b); // Devuelve verdadero si a es menor que b
    boolean esIgual(Object b); // Devuelve verdadero si a es igual que b
}
